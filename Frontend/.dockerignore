# 의존성 — 이미지 안에서 `npm ci`로 설치하므로 호스트 것을 복사하지 않는다.
# (복사 시 빌드 컨텍스트 770MB 증가 + 플랫폼 불일치 위험)
node_modules

# 빌드 산출물/캐시 — 컨테이너의 `npm run build`가 새로 생성한다.
.next
out
dist
*.tsbuildinfo

# 버전 관리 / 메타
.git
.gitignore

# 로그 / 임시
*.log
npm-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db

# 에디터
.vscode
.idea
*.swp

# 테스트 산출물
coverage

# Docker 자체
Dockerfile
.dockerignore
